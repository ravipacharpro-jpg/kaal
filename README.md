# Kaal — samay se aage

Fully autonomous AI agent — Termux / Linux / macOS / Windows.
Premium Rich TUI, Todo + 1-line live status, no code dump.

## Run
```bash
pip install -r requirements.txt
python3 -m kaal "task yahan likho"
```

## Repo
Fresh repo: `kaal`
